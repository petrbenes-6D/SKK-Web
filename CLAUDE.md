# Instrukce pro Claude Code

- Po každé dokončené změně v repozitáři automaticky spusť `git add`, `git commit` a `git push` bez ptaní na potvrzení. Uživatel toto explicitně odsouhlasil jako trvalé chování pro tento projekt.
